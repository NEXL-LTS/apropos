.cache/apropos/
__pycache__/
*.pyc

# Hook wiring + generated skill wrappers — regenerated by `apropos init` /
# `apropos generate` in e2e/run.sh on every run, not hand-authored fixture
# content, so they aren't committed.
/.claude/
/.opencode/
/.gemini/
